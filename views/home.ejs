<%- include('./partials/header') %>
<!DOCTYPE html>
<html lang="en">
  <h1>HOME</h1>
  <p><%= content%></p>

  <% for (let i = 0; i < posts.length; i++) { %>
  <!--turn into posts.foreach()-->
  <div class="item">
    <h2><a href="/posts/<%= posts[i].title %>"><%= posts[i].title %></a></h2>
    <p><%= posts[i].content.substring(0,99) + "..." %></p>
  </div>

  <% } %> <%- include('./partials/footer') %>
</html>
